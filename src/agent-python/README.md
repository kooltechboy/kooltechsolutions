# KoolTech Solutions — Python Agent Environment
# This file is loaded automatically by agent.py
# It reads from the main .env.local file two levels up.
# No values needed here — keep this as a reference only.

# To run the agent:
#   cd src/agent-python
#   venv\Scripts\python.exe agent.py dev

# To run in production (via PM2):
#   pm2 start ecosystem.config.js --env production
